// This program subtracts the difference of two values and displays the result with an appropriate message.

#include <stdio.h>
int main(void)
{
    int difference;

    difference = 87 - 15;
        printf ("The difference of 87 and 15 is %i\n", difference);

   return 0;
}